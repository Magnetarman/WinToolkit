# GitHub Actions Workflow per Continuous Integration con Test PowerShell
# Questo workflow si attiva solo quando vengono modificati file .ps1 nella cartella /tool
# Compila automaticamente WinToolkit.ps1 e testa il risultato prima di committare

name: CI_UpdateWinToolkit_Dev

# Configurazione del trigger: si attiva solo su modifiche a file .ps1 nella cartella /tool
on:
  push:
    branches: [Dev]
    paths:
      - "tool/*.ps1"
      - "WinToolkit-template.ps1"
      - "compiler.ps1"

# Definizione dei job del workflow
jobs:
  # Job principale che gestisce compilazione, test e commit automatico
  compile_test_and_commit:
    # Esecuzione su macchina virtuale Windows (ultima versione stabile)
    runs-on: windows-latest

    # Configurazione dei permessi necessari per il workflow
    permissions:
      contents: write
      actions: read

    # Definizione dei passi sequenziali del job
    steps:
      # Step 1: Checkout del codice sorgente con fetch completo per analizzare i commit
      - name: Checkout del repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      # Step 2: Verifica se ci sono modifiche rilevanti ai file PowerShell
      - name: Verifica modifiche rilevanti
        id: check_changes
        shell: pwsh
        run: |
          Write-Host "🔍 VERIFICA MODIFICHE RILEVANTI" -ForegroundColor Cyan
          Write-Host "=" * 60 -ForegroundColor Yellow

          # Lista dei file che dovrebbero triggerare il workflow
          $relevantPaths = @(
            "tool/*.ps1",
            "WinToolkit-template.ps1",
            "compiler.ps1"
          )

          # Ottieni i file modificati nell'ultimo commit
          $changedFiles = git diff --name-only HEAD~1 HEAD

          if ($changedFiles.Count -eq 0) {
            Write-Host "ℹ️ Nessun file modificato nel commit corrente" -ForegroundColor Yellow
            Write-Host "✅ Workflow completato senza azioni necessarie" -ForegroundColor Green
            exit 0
          }

          Write-Host "📋 File modificati nell'ultimo commit:" -ForegroundColor Green
          $changedFiles | ForEach-Object { Write-Host "  - $_" -ForegroundColor Gray }

          # Verifica se almeno uno dei file modificati è rilevante
          $hasRelevantChanges = $false
          foreach ($file in $changedFiles) {
            foreach ($pattern in $relevantPaths) {
              if ($file -like $pattern) {
                $hasRelevantChanges = $true
                break
              }
            }
            if ($hasRelevantChanges) { break }
          }

          if (-not $hasRelevantChanges) {
            Write-Host "ℹ️ Nessuna modifica rilevante per questo workflow" -ForegroundColor Yellow
            Write-Host "✅ Workflow completato senza azioni necessarie" -ForegroundColor Green
            exit 0
          }

          Write-Host "✅ Modifiche rilevanti rilevate - procedo con compilazione e test" -ForegroundColor Green
          Write-Output "has_relevant_changes=true" >> $env:GITHUB_OUTPUT

      # Step 3: Configurazione ambiente PowerShell
      - name: Setup PowerShell
        shell: pwsh
        run: |
          Write-Host "🔧 CONFIGURAZIONE AMBIENTE POWERSHELL" -ForegroundColor Cyan
          $PSVersionTable
          Write-Host "✅ PowerShell configurato" -ForegroundColor Green

      # Step 3b: Test sintassi file sorgenti PRIMA della compilazione
      - name: Test Sintassi File Sorgenti
        if: steps.check_changes.outputs.has_relevant_changes == 'true'
        shell: pwsh
        run: |
          Write-Host "🔍 TEST SINTASSI FILE SORGENTI" -ForegroundColor Cyan
          Write-Host "=" * 70 -ForegroundColor Yellow

          $hasErrors = $false
          $errorDetails = @()

          # Test compiler.ps1
          Write-Host "`n📄 Test: compiler.ps1" -ForegroundColor Blue
          if (Test-Path "compiler.ps1") {
            try {
              $content = Get-Content -Raw -Path "compiler.ps1" -ErrorAction Stop
              $parseErrors = $null
              $null = [System.Management.Automation.Language.Parser]::ParseInput($content, [ref]$null, [ref]$parseErrors)

              if ($parseErrors.Count -gt 0) {
                Write-Host "  ❌ Errori di sintassi trovati:" -ForegroundColor Red
                foreach ($error in $parseErrors) {
                  $errorMsg = "  → Linea $($error.Extent.StartLineNumber), Colonna $($error.Extent.StartColumnNumber): $($error.Message)"
                  Write-Host $errorMsg -ForegroundColor Red
                  $errorDetails += "compiler.ps1: $errorMsg"
                }
                $hasErrors = $true
              } else {
                Write-Host "  ✅ Sintassi corretta" -ForegroundColor Green
              }
            } catch {
              Write-Host "  ❌ Errore lettura file: $($_.Exception.Message)" -ForegroundColor Red
              $errorDetails += "compiler.ps1: Errore lettura - $($_.Exception.Message)"
              $hasErrors = $true
            }
          }

          # Test WinToolkit-template.ps1
          Write-Host "`n📄 Test: WinToolkit-template.ps1" -ForegroundColor Blue
          if (Test-Path "WinToolkit-template.ps1") {
            try {
              $content = Get-Content -Raw -Path "WinToolkit-template.ps1" -ErrorAction Stop
              $parseErrors = $null
              $null = [System.Management.Automation.Language.Parser]::ParseInput($content, [ref]$null, [ref]$parseErrors)

              if ($parseErrors.Count -gt 0) {
                Write-Host "  ❌ Errori di sintassi trovati:" -ForegroundColor Red
                foreach ($error in $parseErrors) {
                  $errorMsg = "  → Linea $($error.Extent.StartLineNumber), Colonna $($error.Extent.StartColumnNumber): $($error.Message)"
                  Write-Host $errorMsg -ForegroundColor Red
                  $errorDetails += "WinToolkit-template.ps1: $errorMsg"
                }
                $hasErrors = $true
              } else {
                Write-Host "  ✅ Sintassi corretta" -ForegroundColor Green
              }
            } catch {
              Write-Host "  ❌ Errore lettura file: $($_.Exception.Message)" -ForegroundColor Red
              $errorDetails += "WinToolkit-template.ps1: Errore lettura - $($_.Exception.Message)"
              $hasErrors = $true
            }
          }

          # Test tutti i file .ps1 nella cartella tool
          Write-Host "`n📁 Test: File in cartella tool/" -ForegroundColor Blue
          $toolFiles = Get-ChildItem -Path "tool" -Filter "*.ps1" -ErrorAction SilentlyContinue

          foreach ($file in $toolFiles) {
            Write-Host "`n  📄 Test: $($file.Name)" -ForegroundColor Cyan
            try {
              $content = Get-Content -Raw -Path $file.FullName -ErrorAction Stop
              $parseErrors = $null
              $null = [System.Management.Automation.Language.Parser]::ParseInput($content, [ref]$null, [ref]$parseErrors)

              if ($parseErrors.Count -gt 0) {
                Write-Host "    ❌ Errori di sintassi trovati:" -ForegroundColor Red
                foreach ($error in $parseErrors) {
                  $errorMsg = "    → Linea $($error.Extent.StartLineNumber), Colonna $($error.Extent.StartColumnNumber): $($error.Message)"
                  Write-Host $errorMsg -ForegroundColor Red
                  $errorDetails += "$($file.Name): $errorMsg"
                }
                $hasErrors = $true
              } else {
                Write-Host "    ✅ Sintassi corretta" -ForegroundColor Green
              }
            } catch {
              Write-Host "    ❌ Errore lettura file: $($_.Exception.Message)" -ForegroundColor Red
              $errorDetails += "$($file.Name): Errore lettura - $($_.Exception.Message)"
              $hasErrors = $true
            }
          }

          # Riepilogo errori
          if ($hasErrors) {
            Write-Host "`n╔═══════════════════════════════════════════════════════╗" -ForegroundColor Red
            Write-Host "║           ❌ ERRORI DI SINTASSI TROVATI              ║" -ForegroundColor Red
            Write-Host "╚═══════════════════════════════════════════════════════╝" -ForegroundColor Red
            Write-Host "`n📋 RIEPILOGO ERRORI:" -ForegroundColor Yellow
            foreach ($detail in $errorDetails) {
              Write-Host $detail -ForegroundColor Red
            }
            Write-Host "`n💡 Correggi gli errori di sintassi prima di procedere" -ForegroundColor Cyan
            exit 1
          } else {
            Write-Host "`n✅ TUTTI I FILE HANNO SINTASSI CORRETTA" -ForegroundColor Green
          }

      # Step 4: Compilazione WinToolkit.ps1 tramite compiler.ps1 con debugging migliorato
      - name: Compilazione WinToolkit.ps1
        if: steps.check_changes.outputs.has_relevant_changes == 'true'
        shell: pwsh
        run: |
          Write-Host "🔨 COMPILAZIONE WINTOOLKIT.PS1" -ForegroundColor Cyan
          Write-Host "=" * 60 -ForegroundColor Yellow

          # Verifica che compiler.ps1 esista
          if (-not (Test-Path "compiler.ps1")) {
            Write-Error "❌ File compiler.ps1 non trovato"
            exit 1
          }

          # Verifica che WinToolkit-template.ps1 esista
          if (-not (Test-Path "WinToolkit-template.ps1")) {
            Write-Error "❌ File WinToolkit-template.ps1 non trovato"
            exit 1
          }

          # Verifica che ci siano file .ps1 nella cartella tool
          $toolFiles = Get-ChildItem -Path "tool" -Filter "*.ps1" -ErrorAction SilentlyContinue
          if ($toolFiles.Count -eq 0) {
            Write-Error "❌ Nessun file .ps1 trovato nella cartella tool"
            exit 1
          }

          Write-Host "📋 File da compilare: $($toolFiles.Count)" -ForegroundColor Green
          $toolFiles | ForEach-Object { Write-Host "  - $($_.Name)" -ForegroundColor Gray }

          # Esegui la compilazione con cattura output dettagliato
          Write-Host "`n🔄 Esecuzione compilazione..." -ForegroundColor Blue
          try {
            # Esegui e cattura tutto l'output
            $output = & ".\compiler.ps1" 2>&1 | Out-String

            # Mostra l'output
            Write-Host $output

            # Controlla il risultato
            if ($LASTEXITCODE -ne 0) {
              Write-Host "`n╔═══════════════════════════════════════════════════════╗" -ForegroundColor Red
              Write-Host "║        ❌ COMPILAZIONE FALLITA                        ║" -ForegroundColor Red
              Write-Host "╚═══════════════════════════════════════════════════════╝" -ForegroundColor Red
              Write-Host "`nExit Code: $LASTEXITCODE" -ForegroundColor Red
              Write-Host "`n💡 Verifica l'output sopra per dettagli sull'errore" -ForegroundColor Cyan
              exit 1
            }
          }
          catch {
            Write-Host "`n╔═══════════════════════════════════════════════════════╗" -ForegroundColor Red
            Write-Host "║     ❌ ERRORE DURANTE LA COMPILAZIONE                 ║" -ForegroundColor Red
            Write-Host "╚═══════════════════════════════════════════════════════╝" -ForegroundColor Red
            Write-Host "`n📋 Dettagli errore:" -ForegroundColor Yellow
            Write-Host "Messaggio: $($_.Exception.Message)" -ForegroundColor Red
            Write-Host "Tipo: $($_.Exception.GetType().FullName)" -ForegroundColor Red
            if ($_.InvocationInfo) {
              Write-Host "Linea: $($_.InvocationInfo.ScriptLineNumber)" -ForegroundColor Red
              Write-Host "Comando: $($_.InvocationInfo.Line)" -ForegroundColor Red
            }
            if ($_.ScriptStackTrace) {
              Write-Host "`nStack Trace:" -ForegroundColor Yellow
              Write-Host $_.ScriptStackTrace -ForegroundColor Red
            }
            exit 1
          }

          # Verifica che il file compilato sia stato creato
          if (-not (Test-Path "WinToolkit.ps1")) {
            Write-Host "`n❌ File WinToolkit.ps1 non creato dalla compilazione" -ForegroundColor Red
            Write-Host "💡 La compilazione potrebbe essere terminata senza errori ma senza produrre output" -ForegroundColor Cyan
            exit 1
          }

          $fileSize = (Get-Item "WinToolkit.ps1").Length
          Write-Host "`n✅ Compilazione completata: $([math]::Round($fileSize/1KB, 2)) KB" -ForegroundColor Green

      # Step 5: Test estesi del file compilato con report errori dettagliato
      - name: Test Estesi WinToolkit Compilato
        id: test_compiled_wintoolkit
        if: steps.check_changes.outputs.has_relevant_changes == 'true'
        shell: pwsh
        run: |
          Write-Host "🔍 TEST ESTESI DEL FILE COMPILATO" -ForegroundColor Cyan
          Write-Host "=" * 70 -ForegroundColor Yellow

          $winToolkitPath = "WinToolkit.ps1"

          # Verifica che il file esista
          if (-not (Test-Path $winToolkitPath)) {
            Write-Error "❌ File WinToolkit.ps1 non trovato"
            exit 1
          }

          Write-Host "📋 Test del file finale: WinToolkit.ps1" -ForegroundColor Green

          $totalErrors = 0
          $totalWarnings = 0
          $testResults = @()
          $criticalErrors = @()

          try {
            $scriptContent = Get-Content -Raw -Path $winToolkitPath -ErrorAction Stop

            # Test 1: Sintassi PowerShell con dettagli completi
            Write-Host "`n🔍 Test 1: Verifica sintassi..." -ForegroundColor Blue
            $parseErrors = $null
            $tokens = $null
            $null = [System.Management.Automation.Language.Parser]::ParseInput($scriptContent, [ref]$tokens, [ref]$parseErrors)

            if ($parseErrors.Count -gt 0) {
              Write-Host "  ❌ Errori di sintassi trovati:" -ForegroundColor Red
              foreach ($error in $parseErrors) {
                Write-Host "    ╔════════════════════════════════════════════════════════════" -ForegroundColor Red
                Write-Host "    ║ ERRORE DI SINTASSI" -ForegroundColor Red
                Write-Host "    ╠════════════════════════════════════════════════════════════" -ForegroundColor Red
                Write-Host "    ║ File: WinToolkit.ps1" -ForegroundColor Red
                Write-Host "    ║ Linea: $($error.Extent.StartLineNumber)" -ForegroundColor Red
                Write-Host "    ║ Colonna: $($error.Extent.StartColumnNumber) - $($error.Extent.EndColumnNumber)" -ForegroundColor Red
                Write-Host "    ║ Messaggio: $($error.Message)" -ForegroundColor Red
                Write-Host "    ║ Severità: $($error.ErrorId)" -ForegroundColor Red
                Write-Host "    ╠════════════════════════════════════════════════════════════" -ForegroundColor Red
                Write-Host "    ║ Codice problematico:" -ForegroundColor Red
                Write-Host "    ║ $($error.Extent.Text)" -ForegroundColor Red
                Write-Host "    ╚════════════════════════════════════════════════════════════" -ForegroundColor Red

                $errorMsg = "WinToolkit.ps1 Line $($error.Extent.StartLineNumber): $($error.Message)"
                $criticalErrors += $errorMsg
              }
              $totalErrors += $parseErrors.Count
              $testResults += "❌ Sintassi: $($parseErrors.Count) errori"
            }
            else {
              Write-Host "  ✅ Sintassi OK" -ForegroundColor Green
              $testResults += "✅ Sintassi: OK"
            }

            # Test 2: Verifica funzioni disponibili
            Write-Host "`n🔍 Test 2: Verifica funzioni disponibili..." -ForegroundColor Blue
            $functions = [System.Management.Automation.Language.Parser]::ParseInput($scriptContent, [ref]$null, [ref]$null) |
                        Where-Object { $_.GetType().Name -eq "FunctionDefinitionAst" }

            # Tutte le funzioni sono opzionali - verifica solo cosa è presente
            $expectedFunctions = @("WinRepairToolkit", "WinCleaner", "OfficeToolkit", "SetRustDesk", "WinUpdateReset", "WinReinstallStore", "WinBackupDriver", "GamingToolkit", "WinDriverInstall", "SearchRepair")
            $presentFunctions = $expectedFunctions | Where-Object { $functionName = $_; $functions.Name -contains $functionName }
            $missingFunctions = $expectedFunctions | Where-Object { $functionName = $_; $functions.Name -notcontains $functionName }

            $presentCount = $presentFunctions.Count
            $missingCount = $missingFunctions.Count

            Write-Host "  📊 Funzioni presenti: $presentCount" -ForegroundColor Green
            if ($presentFunctions.Count -gt 0) {
              $presentFunctions | ForEach-Object { Write-Host "    - $_" -ForegroundColor Green }
            }

            if ($missingCount -gt 0) {
              Write-Host "  ⚠️ Funzioni mancanti (in sviluppo o non implementate):" -ForegroundColor Yellow
              $missingFunctions | ForEach-Object { Write-Host "    - $_" -ForegroundColor Yellow }
              $testResults += "⚠️ Funzioni mancanti: $($missingCount) (normale durante sviluppo)"
            }
            else {
              Write-Host "  ✅ Tutte le funzioni implementate presenti" -ForegroundColor Green
              $testResults += "✅ Funzioni: Tutte presenti"
            }

            # Conta come avviso, non come errore
            $totalWarnings += $missingCount

            # Test 3: Struttura del menu
            Write-Host "`n🔍 Test 3: Verifica struttura menu..." -ForegroundColor Blue
            $menuTests = @(
              @{ Pattern = [regex]::Escape("while (`$true)"); Name = "Menu principale" },
              @{ Pattern = "Windows & Office"; Name = "Categoria Windows & Office" },
              @{ Pattern = "Driver & Gaming"; Name = "Categoria Driver & Gaming" },
              @{ Pattern = "Supporto"; Name = "Categoria Supporto" }
            )

            foreach ($test in $menuTests) {
              if ($scriptContent -match $test.Pattern) {
                Write-Host "  ✅ $($test.Name)" -ForegroundColor Green
                $testResults += "✅ $($test.Name)"
              }
              else {
                Write-Host "  ❌ $($test.Name) mancante" -ForegroundColor Red
                $totalErrors++
                $testResults += "❌ $($test.Name) mancante"
                $criticalErrors += "Struttura menu: $($test.Name) mancante"
              }
            }

            # Test 4: Dimensione del file
            Write-Host "`n🔍 Test 4: Verifica dimensione file..." -ForegroundColor Blue
            $fileSize = (Get-Item $winToolkitPath).Length
            $fileSizeKB = [math]::Round($fileSize/1KB, 2)

            if ($fileSize -lt 10000) {
              Write-Host "  ❌ File troppo piccolo: $($fileSize) bytes" -ForegroundColor Red
              $totalErrors++
              $testResults += "❌ Dimensione: $fileSizeKB KB (troppo piccolo)"
              $criticalErrors += "Dimensione file sospetta: $fileSizeKB KB (minimo atteso: ~10 KB)"
            }
            else {
              Write-Host "  ✅ Dimensione OK: $fileSizeKB KB" -ForegroundColor Green
              $testResults += "✅ Dimensione: $fileSizeKB KB"
            }

            # Test 5: Encoding UTF-8
            Write-Host "`n🔍 Test 5: Verifica encoding..." -ForegroundColor Blue
            $encoding = [System.Text.Encoding]::GetEncoding('UTF-8')
            $preamble = $encoding.GetPreamble()
            $fileBytes = Get-Content $winToolkitPath -AsByteStream -ReadCount 0

            if ($fileBytes.Length -ge 3 -and $fileBytes[0] -eq $preamble[0] -and $fileBytes[1] -eq $preamble[1] -and $fileBytes[2] -eq $preamble[2]) {
              Write-Host "  ✅ Encoding UTF-8 con BOM" -ForegroundColor Green
              $testResults += "✅ Encoding: UTF-8 con BOM"
            }
            else {
              Write-Host "  ⚠️ Encoding senza BOM (accettabile)" -ForegroundColor Yellow
              $testResults += "⚠️ Encoding: Senza BOM"
            }

          }
          catch {
            $errorMsg = "ERRORE DURANTE I TEST"
            Write-Host "    ╔════════════════════════════════════════════════════════════" -ForegroundColor Red
            Write-Host "    ║ $errorMsg" -ForegroundColor Red
            Write-Host "    ╠════════════════════════════════════════════════════════════" -ForegroundColor Red
            Write-Host "    ║ Messaggio: $($_.Exception.Message)" -ForegroundColor Red
            Write-Host "    ║ Tipo: $($_.Exception.GetType().FullName)" -ForegroundColor Red
            if ($_.InvocationInfo) {
              Write-Host "    ║ Linea: $($_.InvocationInfo.ScriptLineNumber)" -ForegroundColor Red
              Write-Host "    ║ Comando: $($_.InvocationInfo.Line)" -ForegroundColor Red
            }
            Write-Host "    ╚════════════════════════════════════════════════════════════" -ForegroundColor Red

            $totalErrors++
            $criticalErrors += "Test Error: $($_.Exception.Message)"
            $testResults += "❌ Errore test: $($_.Exception.Message)"
          }

          # Output risultati test
          Write-Host ""
          Write-Host "📊 RIEPILOGO TEST:" -ForegroundColor Cyan
          $testResults | ForEach-Object { Write-Host "  $_" -ForegroundColor Gray }

          # Valuta i risultati: procedi sempre se non ci sono errori di sintassi/struttura
          # Le funzioni mancanti sono normali durante lo sviluppo e non bloccano il workflow
          if ($totalErrors -gt 0) {
            Write-Host "`n╔═══════════════════════════════════════════════════════╗" -ForegroundColor Red
            Write-Host "║           ❌ TEST FALLITI                             ║" -ForegroundColor Red
            Write-Host "╚═══════════════════════════════════════════════════════╝" -ForegroundColor Red
            Write-Host "`n📋 ERRORI CRITICI RILEVATI: $totalErrors" -ForegroundColor Red

            if ($criticalErrors.Count -gt 0) {
              Write-Host "`n🔍 DETTAGLI ERRORI:" -ForegroundColor Yellow
              foreach ($error in $criticalErrors) {
                Write-Host $error -ForegroundColor Red
              }
            }

            Write-Host "`n💡 Suggerimenti:" -ForegroundColor Cyan
            Write-Host "  • Verifica la sintassi del file compilato" -ForegroundColor White
            Write-Host "  • Controlla i file sorgenti in /tool per errori logici" -ForegroundColor White
            Write-Host "  • Assicurati che il template sia corretto" -ForegroundColor White

            Write-Output "tests_passed=false" >> $env:GITHUB_OUTPUT
            exit 1
          }
          else {
            Write-Host "`n✅ TUTTI I TEST SUPERATI!" -ForegroundColor Green
            if ($missingCount -gt 0) {
              Write-Host "ℹ️ Nota: $missingCount funzioni mancanti - normale durante sviluppo" -ForegroundColor Yellow
            }
            Write-Output "tests_passed=true" >> $env:GITHUB_OUTPUT
          }

      # Step 6: Commit automatico del file compilato (solo se i test passano)
      - name: Commit WinToolkit.ps1
        if: steps.check_changes.outputs.has_relevant_changes == 'true' && steps.test_compiled_wintoolkit.outputs.tests_passed == 'true'
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: |
            🔧 Update WinToolkit.ps1 - Auto-compiled and tested

            • Compilazione automatica da file in /tool
            • Test di sintassi e struttura passati
            • Tutte le funzioni sono opzionali e in continuo sviluppo
            • File aggiornato e funzionale
          file_pattern: WinToolkit.ps1
          branch: Dev

      # Step 7: Output finale del workflow
      - name: Risultato Workflow
        shell: pwsh
        run: |
          Write-Host ""
          Write-Host "╔══════════════════════════════════════════════════════════╗" -ForegroundColor Green
          Write-Host "║                 🎉 WORKFLOW COMPLETATO!                  ║" -ForegroundColor Green
          Write-Host "║                                                          ║" -ForegroundColor White

          if ("${{ steps.check_changes.outputs.has_relevant_changes }}" -eq "true") {
            Write-Host "║  ✅ Modifiche rilevanti rilevate                         ║" -ForegroundColor Green
            Write-Host "║  🔨 Compilazione eseguita                               ║" -ForegroundColor Green

            if ("${{ steps.test_compiled_wintoolkit.outputs.tests_passed }}" -eq "true") {
              Write-Host "║  ✅ Tutti i test superati                               ║" -ForegroundColor Green
              Write-Host "║  📝 Commit automatico eseguito                          ║" -ForegroundColor Green
              Write-Host "║                                                         ║" -ForegroundColor White
              Write-Host "║  🚀 WinToolkit.ps1 aggiornato e pronto!                 ║" -ForegroundColor Cyan
            }
            else {
              Write-Host "║  ❌ Test falliti - commit non eseguito                  ║" -ForegroundColor Red
              Write-Host "║                                                         ║" -ForegroundColor White
              Write-Host "║  💡 Verifica i dettagli degli errori sopra              ║" -ForegroundColor Cyan
            }
          }
          else {
            Write-Host "║  ℹ️ Nessuna modifica rilevante                          ║" -ForegroundColor Yellow
            Write-Host "║  ⏭️ Workflow completato senza azioni                   ║" -ForegroundColor Yellow
          }

          Write-Host "╚══════════════════════════════════════════════════════════╝" -ForegroundColor Green
